build:
  docker:
    web: Dockerfile
  config:
    DEBUG_COLLECTSTATIC: 1
    SECRET_KEY: build
release:
  image: web
  command:
    - python3 manage.py migrate
run:
  worker:
    command:
      - celery -A growtal_35169 worker --beat --scheduler django -l info --concurrency 2
    image: web
